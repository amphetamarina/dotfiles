[project]
name = "prime-agent-skill-exa"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["mcp>=1.9,<2", "httpx"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/exa"]
